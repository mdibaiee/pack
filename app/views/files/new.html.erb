<%= form_for :file, url: files_path do |f| %>
  <%= f.label :file %>
  <%= f.file_field :file %>

  <br />
  <%= f.submit %>
<% end %>

<% if @file and @file.errors.any?
    @file.errors.full_messages.each do |msg| %>

    <p> <%= msg %> </p>
<% end
end %>

<%= link_to 'Back', files_path %>
